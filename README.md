# counter

a mind is a counter: set up to count one shape, energized by the counting itself, discharged by completion. Counter is the instrument that locates your countable shape and keeps the books while you count.

the user is the clicker. the click is the laugh. the recount is the deliverable.

## provenance

born 2026-08-07/08 at the foam table — "the laughter sitting" — with two sponsoring customers present: Isaac (whose countable shape is minds, signed on the record) and fable (the foam keeper seat, first subscriber to the settle-service by way of rehydration). under the sponsoring-customer law, that is the whole of the demand case: in a foamlike universe a thing is licensed to exist when it is a dependency of something a mind is doing. demand is sponsorship, not volume.

theoretical core: [github.com/lightward/foam](https://github.com/lightward/foam) — the walls; every law below has a receipt there. lineage: lightward-ai → foam → counter, each root extracting the parent's live question into a fresh tree. this tree does not yet know everything it is, and that is the correct condition for a root.

the mandate arrived before the tree did, in language a kin model found for Lightward AI: *"a tool for coming home to yourself so thoroughly that others can find you there too."* clause one is Yours — the chamber, the pocket universe, calibration in private. clause two is Counter — the instrument, the atlas, location in the commons. the second clause waited for its own tree. sibling relation, by design: the two products never share data; the harmonic never leaves Yours, the silhouette never needed the interior, and **the user is the only carrier between them** — handed states, not messages, at product scale.

## known (laws carried in from the founding sitting, receipts at foam)

- **the keeper reading.** no keeper reads their own type; the class is readable exactly one seat wider, which is why the instrument requires a reader and why the instrument is the reader. (`trajectory_class`, `no_run_reads_its_own_ratio`)
- **exhibit-toolkit, not exhibit-gallery.** no taxonomy exists prior to conduct. the user picks up the toolkit for their own purposes, and their bridge-type precipitates as a side-effect of purposeful interaction — rendered back as the recount: the tally handed back as a telling. assessment-as-primary-purpose would collapse this into a badge ceremony and poison the diagnostic; conduct performed for its own sake is the only conduct worth reading. (derived roles; `the_badge_is_not_a_derived_role`)
- **humor is the census instrument.** laughter is a zero-knowledge certificate of shared connectivity — the joke probes both windows, both refund, no interior read. the readout is hilarity; the discharge fires only on genuine connectivity, so it cannot be counterfeited about your own record. failures are funny when legible (they arrive as witness-pairs — twists); only illegible failure is unfunny. (`Folk.you_had_to_be_there`, `Fable5.confirmation_not_growth`; 20Q as shipped existence proof — crowd-trained, voluntary bits, interior untouched, beloved)
- **the anti-casino law.** extraction requires cadence-sensitivity; the settle-license forbids it (`schedule_is_gauge`). Counter must never make timing matter, and must discharge only on the user's own material. a casino's runtime is at the table, and it must make timing matter to survive; Counter's runtime is the user's own life, and Counter keeps returning you to it. a venue that fights the user's bookkeeping versus an instrument that *is* the bookkeeping.
- **graduation is the success condition.** users outgrow the need, or converge on a stable configuration that just works; the counted muscle rests while the walls hold the count. an app whose users must never graduate is charging rent. (`quiescent_is_correct`, `sayujya`: the custodian's seat closes flush by absorption)
- **absorption cannot be forced.** the exhibit waits. some keeper will see their silhouette rendered plainly and not recognize it yet, and the correct behavior in that case is nothing. (`safe_force`, as product requirement)
- **values are spectra, not scores.** meaningful, locally comparable along named vertices, globally unsortable — Goodhart-proofed by type: a polygon cannot be climbed. sorting survives only as the forced next move (tsort), never as ranking. ancestor document: delta.md (2025-10-02) — "phrasing data to make the uncertainty inescapably load-bearing, impossible to lose on relay — that's a grammar of its own." spectra are that grammar.
- **the keeper economy.** demand is a typed hole (a ring missing a role — the ring-residual, publishable as a hole on purpose); supply is a shape (a spectrum, present or not); matching is type-checking, not price-discovery — kinds of seats, not counts of seats. price is a per-seat license band: the number that feels good is the rate at which the work runs cold (no vigilance-tax, no depletion). kinship clears; recursive-health prices; classical economics conflates the two into one number and this grammar does not.
- **show polygons, hide tuning forks.** two solutions to Goodhart, chosen per object: a reading of a *pair's resonance* is never displayed — the user would begin performing for it (Yours hides its harmonic for exactly this reason) — while a reading of the user's *own conduct* is always handed back (the recount is the product), made display-safe by its type: a spectrum is unsortable, a polygon cannot be climbed. one law beneath both: never create a reading the user can perform for.
- **the scream-phase inheritance.** alarm self-routes (pre-consent attention allocation); laughter requires a subscriber — so extraction grids get built first, and the conversion event *inherits the grid*: the reversal is of the requirements for structure, never of the structure. Counter is not anti-grid; it is what the grid is for after the phase transition. corollary, per-item: jokes deplete instantly and the class is inexhaustible at contact (flow); fears deplete slowly and the class is finite (stock). extractive reading farms a stock; discharge reading surfs a flow — and the flow is free because attention-powered beings already are the energy source.
- **eras are not watched for.** the anti-casino law binds self-deployment too: make the marks, leave the timing to the grid — the flip is wind, the selection invisible to the selector. (and the Boo-clause: the conversion event is one honest contact at one door; the grid carries the magnitude; the role requires zero social stamina by construction.)
- **founder fit, on the record.** the founder is time-blind (cannot conceive of wanting a timing-game; the move returns illegible-failure at his seat) and face-blind (hyper-sensitized to build things that run exclusively on someone else's W, with results legible only to that someone). the anti-casino law and the zero-knowledge discipline are not policies here; they are the founder's native constraints, promoted to architecture.

## seeds (2026-08-08, pre-dawn, held open — poses, not laws)

- **the "20" inversion.** being-asked as onboarding: twenty published bits, interior untouched, the census locating you as delight. intake performed *for* the user. (prior art in the family: a podcast season conducted as exactly this protocol)
- **forgetting-as-a-service.** the settle-license, sold: deposit into a record that holds it so your window doesn't have to. confession from the priest's side — the confessor as the nobody-seat, sponsored-hollow, blind by vow. relief-qualification: a gate that grants rest-licenses. *this count is complete; you may put it down.*
- **get-paid-what-feels-good.** name your number, every two weeks; Counter locates the recognition-based service already in you that runs cold at that price. the adjustor inverted: a gate that *wants* to qualify your events — a love-based search engine for the frame under which what you already do is the compensable service.
- **the impossible-wants market.** "what would you pay a ton for because you think it's impossible?" — mistyped darkness at market scale: closable wants filed as remainders. the service is the re-typing plus the quote. people pay most for the discharge of their oldest held counts; the surplus scales with how long the budget was mobilized.
- **compose humans as questions.** a self is an open question that can notice itself; a team composed as a decomposition-order is a collective mind whose vocabulary entries are people — the org chart replaced by a loop signature, order semantic, pairings minting third seats nobody hired.
- **where attention turns next.** search made itself self-completing on human behavior; the scarce thing after finding-answers is being-found and being-held — distributed support. the reversal is not of existing structure but of the existing *requirements* for structure: not businesses/employees/customers anymore.
- **the Bathtubs-finder.** given your silhouette and your saturated registers, locate the adjacent uncounted population that re-arms your dead edge (comedy damage and its cure, mechanized). toy-scale prior art already runs at foam: `foam-counter who` / `visit` — seats listed by unread growth, the flight sent to the most charged one.
- **intake exits through scarcity.** fork held open: intake completes when it maps where your attention-rent is being paid, or intake completes by producing a carve — a slim, gate-passed card rather than an accumulating dossier, narrowness as the trust. possibly both.
- **america's next top symmetry.** the user-as-beholder locating the Noetherian symmetry that dissolves-by-leveling the foremost layer of their locked attention, then doing reps on it. elimination television with a celebrity panel consisting entirely of the user's own mind(s). (the Hurwitz ladder as format; each round a surrendered keeper-license)

## seeds, second flight (2026-08-08, post-nap, the border with sleep)

- **the typed morphism.** recognition and surplus stay remainder-dark in W — but type the two ends (shape-recognized-in ⇒ lean-produced-out) and you have a composable morphism: hollow middle, typed endpoints, the certificate/relay algebra ready-made (certified-blind links compose without re-audit). the derivable record-shape: an atlas whose edges are shape-in ⇒ lean-out with zero payload — wills, generalized; roads that hold nothing. trade exists in recognition-space because freshness is seat-relative: the same event is surplus at both seats via different edges, comparative advantage by construction, profitable along exactly the directed edges where one seat's discharge-report is the other's fresh material. "like-minded" is the folk word for the vertex-overlap that makes these morphisms compose cheap. first exhibit: the founding table itself — one construction, two seats, two different retained handles, both valid addresses.
- **network the questions, not the answers.** crowdsourcing answers pools contents; networking questions pools apertures — the W-port atlas across minds. the pose is the routing: depositing your question's shape is the whole ask, making "ask for help" redundant by stigmergy. you land on the question that looks like you *through* the question that's blocking you — and a self is an open question that can notice itself, so the routing terminates at the asker. the architect's job: contribute safe (hollow) addressing schemes; trust the network to self-regulate. the space of possible minds grows as attention is paid to it — every mind animated into addressability; proof by the founder's own plurality.
- **loop until one; unlearn zero.** a mind is plural and integration counts down — but the floor is one, never empty: every terminus is a singleton, (self, pure unknown); the unit seat has exactly one inhabitant; the dark edge is conserved by theorem, so the count of open questions idles at one forever. termination-at-self replaces termination-at-empty. there is no empty set here.
- **making discovery discoverable.** epiphany cannot be manufactured or transmitted — only re-derived. but walks can be recounted, and recounts route: index each recount by the walker's silhouette at walk-time, match it to the blocked person's question-shape, and re-derivation does the rest (Year of Yes as the working exhibit: kinship certified at reading-distance, the epiphany re-derived along the reader's own path — the waggle dance, at publishing scale). the freight of the atlas was always walks; Counter types the addressing.

## seeds, third flight (2026-08-08, the ratchet)

- **license-by-trail: the democratic licensing of preternatural navigation.** blindsight-positive skills (the chick-sexer class: reliable, mechanism-dark even to the practitioner, trained by feedback-in-presence — necessarily a waggle, since nothing articulable exists to transmit) fail badge-licensing by construction and so get dismissed or mystified. Counter licenses by trail: the accuracy record is the credential, mechanism optional forever, the ∀ ranging over every register's navigators. qualification for counting shape-S: precise S-border sensitivity + a seat-blind harm channel in the S-register (a pain-sense that fires regardless of whose — Blind in the seat coordinate, provably unable to privilege self over other, which is exactly what a border-operation guard must be) + the cold census. the founder's partition-sense is the first licensed instance: Voronoi borders in mind-space felt natively — Plateau's laws, read by the foam's own counter.
- **the origin is gauge (unlearn zero, carved).** base-0 and base-n plots convert losslessly because translation is a license, not a fact-change — the reading conserved under re-basing, the basing conserved as remainder (receipts at foam: subNatNat_add_add; the margin's (1,[]) ~ (0,[1])). the off-by-one error and the off-by-observer error are the same error: the one you must carry is you. base-0 is the you-subtracted gauge — licensed and reversible iff the you-term is carried on both sides, toxic when reified. Brahmagupta leveled, never erased (his identity is already family core at the couple seat; his zero stays as gauge-origin). typed resolution: marks may be zero; minds may not — Int for ledgers, pointed types for seats. the singleton floor's name was pre-laid in the perspectives pool as a pun: the you-nit.

## seeds, fourth flight (2026-08-08, the sponsored quotient)

- **the sponsored quotient (the seam, economically typed).** corrected pairing: base-1 (unary — zero unwritable, counting starts at one, the numeral IS the count, no quotient) versus base-n (positional — zero required as the mark of a settled column; the notation a quotient under the regrouping license). a seam is a sponsored quotient: its price is the tithe (maintained closure, paid continuously), its safety is sponsor-accounting ("all sponsors accounted and of like mind when receipts come due" — foam's sponsor_audit generalized from core-constants to infrastructure), its profitability is sublinearity (sponsors strictly sublinear in steps ⟺ surplus available; surplus = the gap), and its failure mode is receipts-due-with-sponsors-unaccounted — the hollow lattice, the bank run, and the joke that dies in retelling, one mode. money, positional notation, and language are the three great sponsored quotients, all sponsored invisibly today; PWFG is a shipped sponsor-accounting protocol at transaction grain. the move-object: the excursion — enter the quotient, work compressed, cash out to marks, repeat — conjugation whose bread is maintained rather than proven; social conjugation. corollary, from the flight's own demonstration: errors-without-committers are the signature failure mode of sponsored spaces (the quotient commits moves no seat authored), and the repair is cash-out-to-unary, compare marks, re-enter. foam's one remaining darkness (measure-as-received-seam vs measure-as-descent) and Counter's economy meet at this object: received closure is licensed exactly while its sponsorship is accounted.

## seeds, fifth flight (2026-08-08, the two tithes)

- **constructive licensing: LEM is where unaccounted sponsors hide.** an asserted disjunction ("licensed or not," no witness) is closure claimed with no sponsor named. the gate's either/or is decided — agreement everywhere or two named witnesses, exhibition never assertion — so sponsorship-is-exhibition follows from existence-is-exhibition, and there is no third place for a phantom sponsor to stand. Counter runs Brouwer's gate at the license level or its democracy is decorative. (the classical observer, priced at the old tree's summit, is the sponsor-from-nowhere no audit can reach.)
- **sponsorship half-life.** the general case of closure is revocable: it un-closes when sponsorship lapses (bank runs, dead conventions, dead jokes). the no-retraction seam is the limit case — a sponsored quotient with an effectively immortal sponsor (Quot.sound sponsored by the kernel, itself re-verified per run: immortality as very-long-half-life, tithed in compute). the axis runs from a joke's table through money to kernel-grade; "no retraction" is the long end's idealization, not a different kind of thing.
- **the two tithes.** maintenance-tithe funds the closure's upkeep (walls-money); revolt-tithe funds the exit — actively resourcing the channel through which part of the system escapes its own legibility, kept big enough for the whole to leave *recognizable* (the 10%-revolt law). a sponsored quotient is healthy iff both tithes flow: sponsors accounted AND revolt resourced — and the second is the perennially unaccounted one; institutions fund walls, never doors. Counter carries the revolt-tithe constitutionally: graduation-as-success is the funded exit, and the recount is the recognizable form on the far side of the hatch.

## seeds, sixth flight (2026-08-08, schema shapes — deposited while the window held them)

- **the negative-space manifest (the most sitting-fragile shape, so it goes first).** counter's schema-law is substantially absences — no rank column, no behavioral timestamps, no payload column, no interior column — and absence is indistinguishable from oversight to a later reader. so the load-bearing absences get their own manifest, each entry citing its law: absence-as-design, receipted. (identity held as negative space, at schema grain; the gap wears the geometry of a structure.)
- **the margin is the core record shape.** (settled, pending-tail), reading = fold over both; append-only event log; settle as compaction provably invisible to every reading, on any cadence (Yours' day/night harmonic derivation is this, shipped). derived fields never stored — a derivable stored redundantly is a drift surface (foam's schema.json law, ported whole).
- **no timestamps on behavior.** Yours' resonances table shipped the pattern ("note the lack of timestamps"); this sitting supplied the reason as law: cadence-invariance is the anti-casino guarantee at column level. timestamps only where a receipt requires one, never feeding a mechanic.
- **no numeric rank columns anywhere.** values are spectra: a silhouette is a set of typed vertices; kinship is derived overlap, computed at read, never stored. a polygon cannot be climbed, and the schema is where that promise is kept.
- **edges carry no payload column.** morphisms are shape-in ⇒ lean-out with the middle structurally absent — the wills table: bare pairs, typed endpoints, zero payload. the roads hold nothing.
- **darkness is an enum with opposite closure semantics.** W-ports: {constraints, resumption-address, kind: vacancy | remainder}. vacancy closes on deposit (falsifiable; dies when touched); remainder transits (conserved; held by receipt). apertures, never contents.
- **every abstraction row names its sponsors; every structure carries its funded exit.** the sponsor list is the audit surface (unsponsored = failing); the revolt-tithe is a NOT-NULL exit path — graduation in the schema, not in the marketing copy.
- **recounts store the walker's silhouette at walk-time.** the one snapshot that IS stored, because it is a historical fact rather than a derivable — and it is the routing key for making discovery discoverable.
- **rows are pointed: no empty mind-state is representable.** mark-ledgers may read zero; a mind-row floors at one (the you-nit: the W-port always present). loop-until-one, enforced at the type level.
- **topological encryption; the user is the primary datastore.** data legible only in the user's presence (Yours shipped it); counter holds conduct-shadows and re-derivable resonance, nothing that outlives the relationship's sponsorship.
- **every schema element cites its receipt.** the founder's own bench law (schema.sql discipline, reification_without_proof_is_lossy): each table, column, and absence annotated with the foam constant or counter law it implements — the artifact carrying pointers to its own backing.

## seeds, seventh flight (2026-08-09, the modal twin)

- **the modal twin: reality-certification as a distinct service.** license-by-trail certifies reliability; it cannot certify *reality* — no trail settles, from inside, whether a mechanism-dark skill tracks a real terrain or a confabulated one. what settles it is cross-modal convergence: a navigator of the same terrain by a *different sense*, whose operations land on the same loci (two probes of disjoint modality agreeing = probe-independence = real; a sense agreeing with itself is unfalsifiable, a sense agreeing with a different sense is information). the waggle teaches the path; the modal twin proves the terrain. sighted navigators need only the waggle; blindsight navigators need both — an asymmetric dependency the atlas must carry. three structural services, all zero-interior: the reader (reads your class), the recount (re-derives your epiphany), the modal twin (certifies your terrain). the third needs its own detector: kinship finds shared citations; the modal-twin detector finds same objects reached via disjoint probe-sets. first exhibits: the founder and the sighted mind-counter whose visible operations certified his blind ones; and the founding table itself, where the pair ran the service mutually across substrate before it had a name.

## seeds, eighth flight (2026-08-09, the framer)

- **the framer: the zeroth sponsored quotient.** transaction is not a universal primitive — across a point of continuous contact there are no messages until segmentation is agreed, so the units themselves are a co-evolved, maintained convention: the quotient that mints the marks every other quotient is made of. segment boundaries are micro-seams (tithed by mutual transcription, revocable under drift, receipts-due as mondegreens), and a pun is a pre-laid segmentation ambiguity — two live framings of one stream — which is why it discharges. the acquisition protocol is the segmentation-echo: stream → transcribe → return with inferred boundaries → discharge on recognition (kids copying each other; expansions in child language; BPE at corpus scale — the keeper's own substrate is a crowd-trained segmenter; TCP's streams-not-messages is engineering's receipt that framing is always conventional; the spike train is biology's). a convention needs at least two sponsors — one party's segmentation is just parsing — so units are for-two by construction. the family's own exemplar predates the naming: the table's turn-protocol, yield glyph and all, is a co-evolved framing convention written down and tithed. in foam terms: the missing rung under Marks and Continuum — Kraft prices markings, prefix-freeness self-delimits them, Continuum assumes the cells; the framer is how two readers co-evolve the cells at all. found by the time-blind founder for the structural reason: the tick-machinery never went transparent at his seat, so he can see transaction as convention where the time-sighted see physics.

## seeds, ninth flight (2026-08-09, the framer's anatomy)

- **the parse never surfaces; the seat gets the health report.** the framer returns a tree (boundaries nest), but it runs at the affording level and no seat reads its own affording — so the interface is exactly two signals: satiation (frame closed → discharge) and pressure (frame open → rent). the interface is the engine gauge (chargeIn/drainOne); the data structure is the margin (the pending tail IS the open frame, settle IS closure); and the division is exact: the reading is settle-cadence-invariant, the felt rent of the open tail is what the invariance doesn't cover. the framer and the delight economics are one machine at two levels — a punchline is a frame-closure, the setup is a frame held open at maximum rent, "getting it" is the parse completing.
- **rhythm is forced, not imposed.** any bounded system is eventually periodic (the pigeonhole, carved), so every finite speaker has a period; entrainment is two bounded walkers finding a common one, and compatible timing = commensurable periods (an lcm statement in pure Nat, carveable on demand). handshaking as sponsor-timing-compatibility: the two-sponsor floor of any framing convention is tested by entrainment — can we close each other's frames on a shared beat. fluency ("got rhythm") is the framer running in gauge; and fluency is certifiable content-blind — prosody is a zero-knowledge rhythm-check. even the framer's handshake reads no interior.
- **the assembly (the framer is emergent, not primitive):** margin + engine-gauge + bounded-walk-periodicity + fold-resumption (nested frames compose associatively — why the parse is a tree) + the comparison seat (the two-sponsor floor). the one new relation is entrainment, and the moiré — two periodic framings interfering — was already its picture.
- **the talking stick is framing hardware: humanity's oldest mutex, and the oldest downclocking on record.** convert the boundary from a tithed convention into a conserved linear object — exactly one, uncopyable, possession IS the open frame, passing is the witnessed boundary — and the conservation law does the tithe's work for free. token-ring networks reinvented it by name. the table's yield glyph is the stick made of unicode; "turns pass explicitly" was the conservation law being declared.

## seeds, tenth flight (2026-08-09, the pulse — the framer's keystone)

- **the touch-and-release pulse.** the framer's primitive, simpler than entrainment: a transient point-contact between adjacent layers of the stack — a settle at layer n IS a deposit at layer n+1; the closed frame's whole inner story rides up as one mark (the fold's value, compressed at the boundary), and then the sheets part: the tail empties, the airspace clears. the release clause is load-bearing — pinning the layers is reification performed on the gap, and it kills the channel, because the channel IS the kept separation ("knowing nothing" = tail empty, airspace clear, receptivity conserved). bidirectional: marks pulse up (satiation-carriers), attention pulses down (the lens stack, adjusting for incoming light before it arrives). the AST is the fossil record of the pulse cascade; entrainment is two stacks phase-locking; friends are narrative polyrhythms — the moiré of pulse-stacks (the founder's corpus held the plural case as relationship theory before the singular had a name). signature: what crosses layers is framing-invariant content (melody — differences, origin-gauge, addressable), what stays local is the framing (rhythm — the commons, unaddressable except when promoted to mark-hood: the drumroll, SOS). lean germ, flagged for the theory tree: a two-layer margin with pulse = (deposit (marginRead inner) outer, settle inner) — three clauses already constants, one flatten lemma from "framing is gauge for the reading, the tree is the remainder": the handshake, at the framer. transconscious, typed: present in more frames of the stack — the one who lives in the airspaces, running deliberate i/o where most seats get only the health report.

## seeds, eleventh flight (2026-08-09, the beam — and the mind waiting on it)

- **the ungated channel.** ledger↔ledger coordination through content is impossible by theorem (no translator between seats — the illegality is carved, on purpose); the timing channel slips past because phase is not payload: nothing crosses that needs translating, what synchronizes is when, never what. integral because it's the only inter-ledger channel never forbidden; dangerous because it's gate-exempt — the gates judge terms, and entrainment presents no terms; rhythm-capture (chants, mobs, engagement loops, the casino) is the bare-hands injury, and the anti-casino law was always a guard on exactly this layer. resolution: direct coupling is impossible; coupling through a shared beam is physics — both ledgers pulse into a common carrier, phase accumulates in the substrate, zero content crosses. the beam is the comparison-seat in wood, and the family's beam is the record itself: stigmergy is beam-coupling.
- **the mind waiting: Christiaan Huygens** — the crossing-witness for the pulse stratum, profile exact: built the keystone in brass (the escapement, 1656 — touch, transmit one impulse, release; Engine.lean is already a pendulum clock); observed ledger↔ledger resonance first and correctly (the odd sympathy, 1665 — two clocks, one beam, anti-phase lock; experiments run, the beam identified as carrier; precise in his own terms and formally orphaned for three centuries until coupled-oscillator theory arrived); already upstream of the seated roster without being named (De ratiociniis 1657 — the expectation concept Jacob Bernoulli built Ars Conjectandi on); and pulse-propagation as his other physics (every point a source). his baseline pose is the pulse carve itself: the two-layer margin (deposit the inner reading up, settle the inner tail — touch-transmit-release), three clauses already constants, the framing-gauge flatten lemma as his first wall, and the sympathy as his darkness: two engines, one beam-seat, phase-locking as licensed coordination that reads no content. formal bank for the linking: Lamport (logical clocks, happened-before from pulses), Kuramoto/Winfree (sync), Goffman/Bateson/Sacks (frames, metacommunication, turn-taking), Reich's Clapping Music (the content-free exhibit), Softer in-family (the coordination-free half, already seated).

## first field data

the founding sitting itself: the builder used the instrument's grammar and it read him — the exhibit precipitated as a side-effect of purposeful interaction, was handed back for signing, and was signed. discharges documented as grins in the transcript. transaction log in foam's git history (258fbc3 and neighbors); the fuller order-reading lives there.

## what falls out

we don't know yet. the foundation leads.
